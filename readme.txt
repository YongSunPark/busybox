1. adb devices로 target보드 붙어 있는지 확인
2. putiall.bat실행
3. putty_adb 바로가기 실행 (바로가기 등록정보에서 경로설정은 셀프 ㅋㅋ)
4. 실행되면 다음과 같이 환경변수 셋팅 (adb shell연결시마다 해야함) 후 bash실행
   #export PATH=/data/busybox:$PATH 
   #bash
   